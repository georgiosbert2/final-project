#include <iostream>
using namespace std;

int main() {
    int n = 5;
    int arr[n];

    // Example usage of an array and a loop to print all elements
    cout << "Array: ";
    for (int i = 0; i < n; ++i) {
        cout << arr[i] << " ";
    }
    cout << endl;

    return 0;
}
